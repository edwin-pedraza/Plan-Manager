# Google Gemini API key (required for AI features)
GEMINI_API_KEY=

# CORS origin for the backend server.
# Use http://localhost:3000 for local Vite dev, or http://localhost:8080 for Docker.
CORS_ORIGIN=http://localhost:3000

# App server port (default: 8080)
PORT=8080
