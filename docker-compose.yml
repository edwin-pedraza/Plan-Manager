services:
  planea:
    build:
      context: .
      target: app
    environment:
      PORT: 8080
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:8080}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
    restart: unless-stopped
